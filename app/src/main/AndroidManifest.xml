<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.manue.rockcalendar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LogoActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ListadoAcontecimientosActivity"
            android:label="@string/title_activity_listado_acontecimientos"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity android:name=".AcercaDeActivity" />
        <activity
            android:name=".BuscarAcontecimientosActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity
            android:name=".MostrarAcontecimientoActivity"
            android:label="@string/title_activity_mostrar_acontecimiento"
            android:theme="@style/AppTheme" />
        <activity android:name=".EventosActivity" />
        <activity
            android:name=".ConfiguracionActivity"
            android:label="@string/title_activity_configuracion"></activity>
    </application>

</manifest>